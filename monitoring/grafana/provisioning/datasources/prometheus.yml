apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: polyglot-prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      # Must match global.scrape_interval so $__rate_interval resolves sensibly.
      timeInterval: 15s
